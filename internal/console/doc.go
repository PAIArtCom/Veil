// Package console is the local, single-user web console (localhost-only): live
// masking activity, local rule/type configuration, key management, and proxy control.
// It reads/writes the local PolicyProvider and reads the local AuditSink counters —
// the same two seams PAIArt's commercial console uses remotely. Frontend assets are
// embedded; the frontend stack is TBD.
//
// Status: scaffold only; no behavior yet.
package console
